/*
 * Shadow header for <net/pkt_sched.h>
 * Linux packet scheduler definitions — not used on FreeBSD.
 * Only needed so cdx_ceetm_app.h compiles.
 */
#ifndef _NET_PKT_SCHED_H_COMPAT_
#define _NET_PKT_SCHED_H_COMPAT_

/* Empty — CDX CEETM code doesn't use any pkt_sched definitions directly */

#endif
